name: Build EXE for Windows

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # Użycie najnowszej wersji

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pandas openpyxl flask

      - name: Build EXE
        run: pyinstaller --onefile --noconsole app.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4  # Poprawiona wersja
        with:
          name: built-exe
          path: dist/app.exe
